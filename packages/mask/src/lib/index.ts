import { generalMask } from './generalMask';

import type { GeneralMaskInterface } from './contracts';

export type TypesMasks = {
  GeneralMaskInterface: GeneralMaskInterface;
};

export const mask = {
  generalMask,
};

export default mask;
